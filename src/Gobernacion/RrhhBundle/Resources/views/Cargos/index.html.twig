<h1>Cargos list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Nombre</th>
            <th>Sueldo</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('Gobernacion_RrhhBundle_Entity__show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.nombre }}</td>
            <td>{{ entity.sueldo }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('Gobernacion_RrhhBundle_Entity__show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('Gobernacion_RrhhBundle_Entity__edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('Gobernacion_RrhhBundle_Entity__new') }}">
            Create a new entry
        </a>
    </li>
</ul>
